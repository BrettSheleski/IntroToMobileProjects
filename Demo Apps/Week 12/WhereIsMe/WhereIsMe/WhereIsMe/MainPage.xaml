<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WhereIsMe"
             x:Class="WhereIsMe.MainPage">

    <StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Latitude: " />
            <Label Text="{Binding Latitude}" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Longitude: " />
            <Label Text="{Binding Longitude}" />
        </StackLayout>
        <Button Text="Update Location" Clicked="UpdateLocationButton_Clicked" x:Name="UpdateLocationButton" />
        <Button Text="Reset" Clicked="ResetButton_Clicked" x:Name="ResetButton" IsEnabled="False" />
    </StackLayout>

</ContentPage>
